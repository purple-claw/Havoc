# render.yaml — Render.com Blueprint for HAVOC backend (free tier)

services:
  - type: web
    name: havoc-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: HAVOC_ENV
        value: production
      - key: HAVOC_CORS_ORIGINS
        value: https://havoc-viz.vercel.app,https://havoc-6qb5grdqe-purple-claws-projects.vercel.app,http://localhost:3000
    healthCheckPath: /health
    autoDeploy: true
